<data>
<${tag}List>
	#foreach($result in $resultList)
	<${tag}>
		#foreach($field in $fieldList)
		<${field}><![CDATA[$!VmUtils.getString($result.get($field))]]></${field}>
		#end
	</${tag}>
	#end
</${tag}List>
</data>
